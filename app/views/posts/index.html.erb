<h1>TOP page</h1>
<%= form_with(model: @post, id: 'form', local: true) do |f| %>
  <%= f.text_field :memo, autofocus: 'on' %>
  <%= f.submit 'post', id: 'form' %>
<% end %>

<% @posts.each do |post| %>
  <div>
    <%= post.memo %>
    <%= post.created_at %>
  </div>
<% end %>